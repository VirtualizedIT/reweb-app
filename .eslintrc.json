{
  "extends": ["next/core-web-vitals", "plugin:tailwindcss/recommended"],
  "settings": {
    "tailwindcss": {
      "config": "tailwind.config.ts"
    }
  },
  "rules": {
    "tailwindcss/classnames-order": "off" // Add this line to disable the rule
  }
}
